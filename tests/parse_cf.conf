# test configuration file for parse_cf
a_allow = true
a_allow_user = daemon ,\
	      nobody
a_deny_user = sys, root
a_allow_group = tty

# Another configuration item
truc_allow_user = root

# Testing groups:
machin_allow_group = root, audio

# Configuration item not in the list ?
bidule = false